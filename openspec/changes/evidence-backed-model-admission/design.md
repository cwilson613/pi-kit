# Design

Admission is a derived projection over `InferenceOffering` and its endpoint. The inventory remains the source of evidence; no independent mutable admission database is introduced. Embedded/declared offerings are curated, discovery-only offerings are provisional, probe-backed offerings are observed, explicit quarantine metadata dominates, and disabled routes are unavailable. Catalog availability is the conjunction of credential gating and admissible route state.
