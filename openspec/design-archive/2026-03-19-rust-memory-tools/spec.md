# Register memory_* agent-callable tools in Rust — Design Spec (extracted)

> Auto-extracted from docs/rust-memory-tools.md at decide-time.

## Decisions

### All 11 memory tools registered (decided)

8 tools were already implemented. Added memory_episodes (backend search_episodes), memory_compact (signals conversation-level compaction), memory_search_archive (FTS search). 11 total tools matching the TS surface.
