# lifecycle-hygiene-verification-substates — Design

## Spec-Derived Architecture

### openspec/lifecycle-status

- **Task-complete changes expose concrete verification substates** (added) — 4 scenarios
- **Missing lifecycle binding is reported as a first-class blocker** (added) — 1 scenarios
- **Binding truth is computed consistently across lifecycle surfaces** (added) — 2 scenarios
- **Coarse stage compatibility is preserved while adding substate detail** (added) — 1 scenarios

## Scope

<!-- Define what is in scope and out of scope -->

## File Changes

<!-- Add file changes as you design the implementation -->
