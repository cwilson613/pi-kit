# Selective Omegon kernel decomposition - Tasks

Dependencies: groups land in order `0 -> 1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7`. Subtasks within a group may overlap only after their shared contracts and red tests exist. Every group retains compatibility adapters until its exit gate passes.

Definition of done for every group: classify documentation impact before implementation; update the owning durable design/developer docs; update applicable `site/src/pages/docs/` pages and canonical `site/snippets/` command examples for operator-visible changes; record why no public change is needed when applicable; and run the narrowest relevant docs/site validation before the group exit gate.

## 0. Design baseline and independent maintenance artifact
<!-- specs: kernel-composition/documentation, kernel-composition/maintenance, kernel-composition/maintenance-protocol-v1 -->

- [x] 0.1 Record the comparative architecture assessment and selective decomposition source design.
- [x] 0.2 Draft the initial proposal, design, normative specs, and ordered implementation tasks.
- [x] 0.3 Specify the maintenance crate topology, command contract, shared exclusion/deny/transaction schemas, mutation roots, deadlines, output schema, dependency posture, and excluded/deferred operations.
- [x] 0.4 Build `omegon-maintenance-contracts` with canonical v1 fixtures and red interoperability, corruption, lock-race, crash-point, path, and outcome tests.
- [x] 0.5 Build the separately runnable maintenance artifact with compiled-profile composition, inert contribution, session-framing, and durable ownership-record diagnostics.
- [x] 0.6a Add inert contribution disable/quarantine, session quarantine, stale-record pruning, and maintenance audit workflows.
- [x] 0.6b Integrate vetted offline Sigstore bundle-v0.3 verification, compiled Fulcio/Rekor trust roots, signed-checkpoint/SET/inclusion-proof validation, and the release fixture matrix before enabling `release verify`.
- [x] 0.7 Integrate and test deny/exclusion locks in every normal contribution startup path, session-deny locks in every resume path, and v1 ownership-record writers.
- [x] 0.8 Package and launch-test source, linked-development, direct-install, platform archive, Homebrew, Nix, and OCI paths supported by the repository.
- [x] 0.9 Prove startup with TUI, default loop, project config/plugins, extension runtime, MCP, mutable packs, memory, lifecycle, and orchestration absent.
- [x] 0.10 Co-deliver maintenance architecture/operator docs, public install/recovery pages, canonical command snippets, and site validation with the artifact.
- [x] 0.11 Remove Homebrew publication as a Slice-zero exit gate. Existing formula verification remains a best-effort packaging safeguard, but publishing a qualifying Homebrew archive is not required to begin later slices. Documentation impact: internal lifecycle policy only; existing public Homebrew guidance and packaging behavior are unchanged.
- [x] 0.12 Make pairwise self-update crash-atomic and rollback-safe through immutable version-directory publication plus one atomic activation switch, with failure injection across activation, launcher, and receipt boundaries. Documentation impact: updated the durable installation/version-switcher contracts, public install guidance, and release notes for `versioned-current-v1`; no canonical command syntax changed.

## 1. Minimum durable session authority
<!-- specs: kernel-composition/documentation, runtime-session/authority -->

- [x] 1.1 Approve the minimum semantic event vocabulary, sequence/version rules, and compatibility policy.
- [x] 1.2 Add the approved adjacent authority stream, strict reducer/cache, and durable prompt, queue, turn, interruption, minimum invocation, recovery, and terminal facts.
- [x] 1.3 Refactor the existing supervisor scaffold into one frontend-neutral compiled implementation instantiated once per session. Documentation impact: internal architecture only; no public commands, site pages, or snippets changed.
- [x] 1.4 Route interactive, ACP, daemon, Web/IPC, and bounded ingress through the owning session supervisor where semantics overlap. Documentation impact: updated private protocol/supervisor/daemon architecture and the public sessions page; no command syntax or canonical snippets changed.
- [x] 1.5 Add a compatibility adapter that submits loop terminal/session intents to the kernel state machine; complete loop reduction in Slice 4. Documentation impact: internal protocol/supervisor ownership only; no public commands, site behavior, or snippets changed.
- [x] 1.6 Add lost/coalesced event, restart, cursor, second-turn, cancellation, and exactly-once terminal regressions. Documentation impact: regression coverage and strict duplicate-event rejection reinforce the existing protocol; no public commands, site behavior, or snippets changed.
- [x] 1.7 Co-deliver session protocol/recovery docs and applicable operator-facing state, resume, cancellation, and client behavior documentation. Documentation impact: corrected private protocol/supervisor/daemon/IPC/Web contracts and public session, migration, and cancellation guidance; no command syntax or canonical snippets changed.

## 2. Composition-authoritative contribution graph
<!-- specs: kernel-composition/documentation, runtime-capabilities/declarations, runtime-contributions/lifecycle -->

- [x] 2.0 Refine Slice-2 ownership, red tests, composition-generation semantics, and the graph-to-legacy dispatch boundary. Documentation impact: internal design/spec clarification only; no public behavior or commands changed.
- [x] 2.1 Add renderer-neutral declaration/generation/diagnostic contracts plus serialization fixtures for dependencies, conflicts, owner tier, trust/confinement, lifecycle, effects, protocol range, timeout, retry, idempotency, transition metadata, and surface support. Documentation impact: updated the owning composition/admission architecture and release notes; this contract-only lane changes no public commands, configuration, site behavior, or canonical snippets.
- [x] 2.2 Add a pure deterministic candidate-graph builder with all-error diagnostics for duplicate IDs/owners, ambiguous invocations, cycles, missing requirements, conflicts, protocol/platform incompatibility, dangling aliases/groups, and undeclared effects. Documentation impact: updated the owning composition/admission architecture and release notes; the pure builder does not change public commands, configuration, site behavior, canonical snippets, activation, or legacy dispatch.
- [x] 2.3 Split static setup into discovery, declaration, validation, activation planning, readiness, and publication phases; adapt existing features before activation and derive legacy EventBus registrations only from the promoted graph. Documentation impact: updated the owning composition/admission architecture and release notes; static publication semantics changed internally without changing public commands, configuration, site behavior, or canonical snippets.
- [x] 2.4 Add versioned non-executable dynamic-contribution preflight and separate trusted-code/confinement admission from capability admission before extension, plugin, script, or MCP code can run. Documentation impact: added the distinct `permissions.trustedContributionCode` operator policy and updated durable composition/admission architecture, public extension/plugin guidance, profile Pkl schema, and release notes; no canonical command or snippet syntax changed.
- [x] 2.5 Add quarantined negotiation, frozen declarations, readiness deadlines, rollback-covered candidate resources, typed health/crash/backoff/degradation/drain/retirement/quarantine/cleanup states, and atomic generation promotion; keep invocation leasing deferred to Slice 3. Documentation impact: updated the owning composition/admission architecture and release notes; dynamic candidates now publish bounded lifecycle and cleanup policy internally, with no public command or configuration syntax change.
- [x] 2.6 Bind new sessions to a composition generation distinct from process instance identity, retain existing generation values as opaque legacy IDs, and expose effective graph, owner provenance, health, denial reasons, cleanup assurance, and compatibility-dispatch status through one shared semantic diagnostic projection. Documentation impact: updated the owning architecture, release notes, and public `/status` reference; command syntax is unchanged, while native and ACP status output now includes composition diagnostics.
- [x] 2.7 Co-deliver contribution authoring, trust/confinement, lifecycle-state, diagnostics, and public extension/plugin documentation. Documentation impact: established one canonical host-runtime guide, corrected the injected authoring contract and stale SDK guides, documented stable trust IDs and source-review semantics in Pkl, and updated validated public extension, plugin, security, and `/status` guidance without changing command syntax.

## 3. Crash-consistent privileged invocation
<!-- specs: kernel-composition/documentation, runtime-invocation/leases -->

- [x] 3.0 Refine Slice-3 ownership, caller/generation identity, red tests, compatibility dispatch, and durable-state boundaries. Documentation impact: updated the internal invocation architecture only; no public behavior, command syntax, retry promise, or recovery guidance changes in this refinement lane.
- [x] 3.1 Move policy/RBAC/approval combination and generation-bound lease issuance into one kernel invocation service. Documentation impact: updated the owning architecture and release notes; model-tool calls now use accepted-graph resolution and generation-bound, exactly-once in-memory leases, while durable invocation facts and other privileged compatibility paths remain assigned to later Slice-3 tasks with no public command or configuration syntax change.
- [x] 3.2 Replace tool-name authority with declared effects, principals, timeout, parallelism, retry, and transaction metadata. Documentation impact: updated the shared declaration contract, owning architecture, and release notes; leased model-tool admission and scheduling now consume validated declaration metadata, while direct compatibility callers and durable invocation state remain assigned to later Slice-3 tasks with no public command or configuration syntax change.
- [x] 3.3 Persist `Prepared` before leasing and `Dispatched` before owner handoff; propagate stable call and deduplication IDs. Documentation impact: updated the durable session protocol, owning invocation architecture, and release notes; authority-backed model-tool calls now persist complete preparation and dispatch identity across interactive, ACP, daemon, and bounded turns, while acknowledgement, terminal settlement, and dispatched-call recovery classification remain assigned to Slice 3.4.
- [x] 3.4 Persist acknowledgement and terminal settlement; recover unsettled dispatched calls as unknown completion. Documentation impact: updated the durable session protocol, owning invocation architecture, and release notes; local, host, extension, and MCP owners now durably acknowledge accepted authority-backed model-tool calls, terminal outcomes settle before completion publication, and unsettled dispatched or acknowledged calls recover as unknown while prepared calls remain unhanded-off. Mutation fencing and emergency recovery evidence remain assigned to Slice 3.5.
- [x] 3.5 Fence further mutation when settlement durability fails and retain emergency recovery evidence. Documentation impact: updated declaration, invocation architecture, durable session protocol, public session/recovery guidance, and release notes; mutating authority-backed model-tool declarations now carry durable domain/key identities, post-dispatch acknowledgement or settlement failures write independent append-only evidence, matching mutations fail closed before preparation, and malformed or unwritable evidence poisons admission. Runtime fence clearing remains unavailable pending deterministic reconciliation or an explicit audited operator recovery contract.
- [x] 3.6 Deny retry of mutating unknown-completion calls without owner-enforced idempotency/deduplication. Documentation impact: updated invocation architecture, durable session protocol, public recovery guidance, and release notes; authority-backed admission now denies session-wide stable-call replay when the original unknown mutating attempt lacked idempotency or exact owner-enforced deduplication, replacement metadata cannot retroactively authorize replay, legacy unknown state fails closed, and ambiguous ACP host writes no longer replay locally. Safe replay scheduling, attempt lineage, and request fingerprints remain unimplemented.
- [x] 3.7 Route tools, actions, extensions, host effects, and privileged internal calls through the same lease path while retaining direct pure/read-only service calls. Admission and lease validation are kind-aware; graph-registered TUI, CLI, ACP, Web, and IPC feature commands use explicit operator scopes and owner-declared surfaces; model-loop path grants, automatic memory ingestion, and host-mediated persona/tone switches use internal leases; managed delegation uses declared service principals and Web/Daemon tool surfaces; operator context-pack reads use a typed read-only service instead of tool dispatch; extension-provided voice stop uses a declared TUI service lease bound to the promoted turn authority; daemon vox polling uses the declared `vox_route` tool under an ephemeral Service/Daemon lease; arbitrary ACP extension calls use an extension-owned conservative Operator/ACP transport lease on the worker-owned EventBus. Lease-less imperative extension HostActions fail closed, operator approval does not widen independent project/runtime/origin-trust gates, and declarative native/MCP HostActions require a live parent guard with effect containment and exactly-once child identity. Documentation impact: updated the invocation architecture, decomposition design, ACP extension hardening boundary, and release notes; no public command syntax or site snippets changed.
- [x] 3.8 Co-deliver invocation/admission developer docs and public permission, retry, unknown-completion, and recovery guidance. Documentation impact: reconciled the OpenSpec lifecycle and normative fence phases, canonical admission/session/extension/recovery docs, and public security, session, recovery, routing, extension, plugin, and command guidance with the implemented lease lifecycle. Known capabilities without configured permission rules retain their policy default, but unknown owners/capabilities/effects fail closed; provider-request retry is explicitly separate from post-dispatch invocation replay; no automatic safe-replay scheduler, historical unknown-invocation listing, or runtime fence-clearing command is claimed. Existing canonical snippets remain accurate because no command syntax changed.

## 4. Provider and loop seams
<!-- specs: kernel-composition/documentation, provider-routing/leases, runtime-session/authority, runtime-invocation/leases -->

- [x] 4.0 Refine Slice-4 provider identity, inventory/evidence ownership, bridge-factory binding, fallback compatibility, route-lease durability, host-adapter boundaries, red tests, and documentation impact. Documentation impact: updated the source design and task boundary only; this refinement changes no runtime behavior, public command/configuration surface, site content, or canonical snippets.
- [x] 4.1 Define provider contributions that bind identity, inventory authority, auth class, schema dialect/tool support, bridge factory, modality/capability evidence, and fallback compatibility. The validated built-in registry uses typed contribution owners/generations, deterministic all-error diagnostics, credential/factory parity, runtime-inventory evidence checks, executable schema normalization, explicit unsupported-tool state, and directed model-family fallback. Known-provider parsing and bridge construction consume the registry; providers without current offering evidence remain diagnosable and ineligible to the future route service rather than receiving fabricated inventory. Added OpenRouter `stealth/ox-alpha` with its published context/output limits, multimodal input, reasoning/tool capabilities, curated menu placement, and stealth retention warning. Documentation impact: updated the Slice-4 source design, provider-route conceptual model matrix, public provider guide, and release notes. No command or configuration syntax changed, so canonical snippets remain unchanged; cross-host route authority and durable route leases remain task 4.2.
- [x] 4.2 Make one provider route service and recorded route lease authoritative across interactive, daemon, child, and bounded execution. `ProviderRouteService` now owns contribution-backed bridge and declared fallback resolution, retains selected and serving model identities on compatibility bridge handles, and supplies the common route metadata used by loop, ACP, daemon, child, bounded, smoke, compaction, and lightweight completion dispatch. Every loop request records a versioned lease before `LlmBridge::stream`: authority-backed requests append `route.lease_recorded` against the active turn, while sessionless requests append the same lease shape to a durable step-owned JSONL stream without inventing session or turn identity. Lease evidence binds provider/model identities, schema dialect or unsupported state, credential-source class, bounded fallback reason, contribution generation, and route policy; stale generations, undeclared fallbacks, partial session scopes, and durability failures fail before provider dispatch. Documentation impact: updated the source design, decomposition guide, durable route boundary, and release notes. Public model-selection/authentication/route-evidence guidance remains task 4.6; no command, configuration, or canonical snippet syntax changed.
- [x] 4.3 Reduce the loop to a release-coupled driver over session, route, context, and invocation contracts. One non-swappable `ReleaseCoupledLoopDriver` now receives required `LoopSessionContract`, `LoopRouteContract`, `LoopContextContract`, and `LoopInvocationContract` ports for every compiled interactive, ACP, daemon, headless/child, bounded, and Sentry turn. The session port captures principal/session/active-turn authority and ephemeral route-step identity; partial, cross-session, or stale authority fails before execution. The route port captures the leased bridge and route controller together, rejects missing or contradictory serving identity except for an explicitly disconnected route, and prevents `LoopConfig` route/invocation authority from bypassing the driver. The driver returns one typed terminal proposal; authority-backed hosts retain cancellation/timeout narrowing and commit it only after host-owned cleanup, while advisory `TurnEnd`/`AgentEnd` remains non-canonical. Documentation impact: updated the source design, decomposition guide, and release notes. Concrete compatibility implementations and policy names remain task 4.4; driver/service replacement and quiescence remain task 4.5; complete message/context/step facts remain Slice 5. No command, configuration, public-site, or canonical-snippet syntax changed.
- [x] 4.4 Remove concrete provider, tool, memory, lifecycle, and frontend names from loop policy. Production `loop.rs` now depends only on the required session, route, context, and invocation contracts for those concerns. Release-coupled compatibility implementations own route setup, provider failure/stream handling, tool admission/batching/owner handoff, permission presentation, memory/lifecycle requests, context assembly/compaction, and plan/recovery/finalization policy outside the loop; one opaque driver binding captures their host configuration. Source guards reject concrete policy regressions and route/invocation adapter callbacks into loop orchestration. Documentation impact: updated the source design, decomposition guide, and release notes. This internal boundary changes no command, configuration, public-site, or canonical-snippet behavior; driver replacement/quiescence remains task 4.5 and complete message/context/tool/step persistence remains Slice 5.
- [x] 4.5 Restrict driver replacement to boot or an explicit quiescent session boundary. One session-lifetime execution owner selects an atomic loop-driver plus provider-route-service pair at boot, captures it at durable turn start, and retains that exact pair through the turn. A mid-turn request returns in-memory `Pending`; ordinary closure and the next turn start never apply it. Only a deliberate caller's explicit `commit_pending_at_quiescence` call may append `session.execution_binding_migrated` and publish the pair, after idle and unresolved-invocation checks. Durable append precedes in-memory publication; unknown completion blocks migration; start-versus-migration coordination cannot produce mixed pairs; legacy replay retains an absent durable binding; and resume boot binding is process-local with no synthetic append. Interactive, ACP, daemon, bounded, headless/child, Sentry, smoke, compaction, and lightweight inference use owner captures or the immutable sessionless boot binding without direct construction. Documentation impact: updated the source design, decomposition guide, durable session protocol, binary-composition boundary, and release notes. This internal boundary changes no command, configuration, public-site, or canonical-snippet behavior. Complete message/context/tool/step persistence remains Slice 5; the separately tracked task 4.6 documentation closeout is now complete below.
- [x] 4.6 Co-deliver provider contribution/routing docs and public model selection, fallback, authentication, and route-evidence guidance. Documentation impact: established `docs/provider-contributions-and-route-leases.md` as the canonical current boundary; reconciled provider credential, fallback, route-state, model-control, session-protocol, error/recovery, extension, and decomposition docs; and updated the public provider, routing, model-control, command, session, recovery, security, install, migration, ecosystem, FAQ, index, extension, plugin, and OpenSpec pages plus the README. Public guidance now distinguishes declarations, inventory evidence, credentials, and executable routes; selected from serving identity; interactive from sessionless fallback; provider retry from invocation replay; and auth/source class from secret material or a precise store. It explicitly records directed non-transitive fallback, non-executable Antigravity, warning-only headless Anthropic subscription use, advisory inventory diagnostics, and the absence of historical route-lease commands or an exhaustive current provider/model inventory. Site assertions cover these boundaries. No command or configuration syntax changed, and `site/snippets/` remains unchanged.

## 5. Complete semantic event spine
<!-- specs: kernel-composition/documentation, runtime-session/authority -->

- [x] 5.0 Freeze the Slice-5.1 authority protocol before production mutation. One `loop.rs` internal iteration is one durable step; context-overflow/history repair creates another request and route lease inside that step. The approved v1 additions are `step.started`, `model.request_prepared`, `model.request_route_joined`, `assistant.content_appended`, `assistant.message_committed`, `provider.continuity_stored`, `tool.call_recorded`, `tool.result_recorded`, `model.request_closed`, `step.closed`, and `step.abandoned`. The protocol fixes content-addressed context/schema/blob identity, UUID/cardinality/ordinal rules, append-before-projection ordering, denied-call authority, invocation linkage, restricted continuity visibility, EOF/cancellation/abandonment, minimum recovery order, legacy behavior, and snapshot/reducer evolution. Documentation impact: updated the OpenSpec design and runtime-session/provider-routing requirements plus the private durable protocol and decomposition guide. This refinement changes no runtime behavior, command/configuration/site surface, or canonical snippet, so no public documentation or Unreleased behavior note changes.
- [x] 5.1 Emit the frozen model-context, request/route join, bounded assistant content/message, restricted provider-continuity, canonical tool call/result, request outcome, and step terminal facts for complete authority-backed sessions only. The version-4 reducer/cache validates exact snapshots, response-attempt lineage, append-before-projection ordering, invocation linkage, and exactly-once terminals. Recovery and interactive/ACP/daemon/bounded abnormal exits classify unresolved handed-off invocations, close any open request with its latest response-attempt identity, append deterministic reason-bound UUIDv5 `step.abandoned`, and then allow host-owned turn closure after cleanup. Interactive cancellation drains the same pinned execution within the supervisor grace period; deadline abort drops its state/authority receivers and joins its turn-scoped event relay before terminal commitment. Detached HTTP work is explicitly externally unverified and cannot append or project after abandonment; process-backed tools retain tree-scoped cancellation/drop cleanup. Partial scopes fail closed; sessionless hosts retain route-only evidence. No tool-progress facts, command/configuration/public-site/canonical-snippet changes, replay-derived provider history/transcripts/frontends/compaction, legacy storage migration, or consumer-recovery claims are included; those remain 5.2-5.5.
- [x] 5.2.0 Freeze full-spine compatibility, read-only replay, response-attempt, provenance, compaction, projection-cursor, recovery, and fixture contracts before task-5.2 runtime mutation. Authority lineages become forward-only after their first full-spine fact; unsupported readers and referenced blob loss fail closed, and neither downgrade nor concurrent old-writer operation is supported. The freeze defines legacy/mixed/full behavior, retry-failure boundaries and legal zero-text tool-call commits, session-scoped idle compaction without fabricated prompt/turn identity, output-before-cursor publication, idle-only atomic context replacement, reducer/cache v5, and the canonical fixture corpus. `route.lease_recorded` v1 and every emitted v1 payload remain unchanged. Documentation impact: updated the OpenSpec design and runtime-session authority requirement plus the private semantic-protocol and decomposition guides. This planning refinement changes no runtime, command, configuration, public-site, canonical-snippet, or release behavior, so no Unreleased note is added.
- [x] 5.2 Extend the Slice-1 compatibility and recovery rules for full-spine context, route, assistant, tool, step, compaction, and projection events without redefining baseline authority semantics. Authority-backed retries durably bound contiguous response attempts, and deterministic recovery resumes after every invocation/request/step prefix while preserving prepared-only invocations as incomplete. Strict read-only replay, reducer/cache v5, the checked JSONL/blob fixture corpus, fail-closed content authorization, forward-only lineage boundaries, event-backed prompt/assistant/tool-result/generated/compaction provenance, compaction authority and recovery, and generic output-before-cursor storage are active. Idle-only atomic replacement shared by interactive, ACP, and daemon controls rejects active execution, unresolved compaction/invocation state, and ambiguous queued prompts; validates recovery, blobs, replay frontier, boot execution binding, and projection identity; and publishes the compatibility conversation and host identity together. Already-durable legacy provenance remains readable, but the full-spine writer no longer emits owner-only transcript fallback. Sessionless execution retains its existing route-only and compatibility-reset behavior without fabricated semantic facts. Documentation impact: reconciled the source design, runtime-session and provider-route specs, semantic protocol, decomposition and binary-composition guides, and release notes. Concrete provider-history, transcript, frontend, and compaction-checkpoint projections remain 5.3; legacy storage/consumer migration remains 5.4; separately designed sessionless semantic lineage is deferred; adverse consumer recovery remains 5.5; final developer/applicable public documentation remains 5.6. No command, configuration, public-site, or canonical-snippet behavior changed.
- [x] 5.3.0 Freeze the four task-5.3 internal projectors and schema v1 before projection implementation. The identities are `session.provider-history`, `session.transcript`, `session.frontend-snapshot`, and `session.compaction-checkpoint`, each at projector version 1 and projection schema version 1. The freeze fixes exact availability envelopes, immutable provider-request inputs, committed-only transcript semantics, durable partial/abandoned frontend evidence, bounded chunk/manifest ownership for provider history and transcript, bounded single outputs for frontend and compaction, deterministic ordering/serialization/digests, mixed-lineage suffix-only exactness and unavailable full-session export, legacy availability-only output, incomplete/abnormal state, restricted-continuity exclusion, coordinator cadence/coalescing, cursor identity, retention, failure behavior, and the shadow-only 5.3 boundary. Fixture/golden coverage is fixed across lineage, request repair/retry, commitment/abandonment, compaction, bounds, determinism, restart, and failure cases. Documentation impact: updated the OpenSpec design and runtime-session authority requirement plus the private semantic-protocol and decomposition guides. This planning-only refinement changes no runtime, consumer, command, configuration, public site, canonical snippet, or release behavior, so no Unreleased note is added.
- [x] 5.3 Derive provider history, transcripts, frontend snapshots, and compaction checkpoints from semantic events. One capacity-one, dirty-bit session worker is owned by each authority-backed supervisor, performs startup/recovery catch-up, coalesces ordinary durable-append hints for 50 ms with a 250 ms maximum delay, and immediately flushes step, turn, compaction, shutdown, and explicit boundaries. Every run strict-replays the latest stable frontier and independently publishes all four frozen projectors; typed worker/projector failures remain observable while prior outputs survive. Authority appends and terminal commitment never await projection work. Atomic replacement clears and stops the old notifier, fences its session-specific root, and transfers its join handle to the new supervisor for owned reaping without delaying host publication; sessionless supervisors start no worker, and source guards keep provider dispatch, `ConversationState`, transcript commands, TUI, ACP, Web, IPC, snapshots, and compaction consumers on compatibility paths. Tests cover burst/lost-duplicate coalescing, latest frontier, startup and crash restart, terminal flush, independent projector failure, nonblocking authority, replacement fencing, sessionless absence, and unchanged consumers. Documentation impact: reconciled the source design, runtime-session requirement/protocol, decomposition and binary-composition guides, and release notes. Consumer migration remains task 5.4; no command, configuration, public-site, or canonical-snippet behavior changed.
- [x] 5.4.0 Freeze task-5.4 authority roles, synchronous current-context reduction, separate host-state and observation durability, validated projection reads, consumer cutovers, replacement publications, compatibility/rollback behavior, command naming, fixtures, and schema-version strategy before migration. Provider dispatch and exact resume require an immutable exact-frontier `CurrentContextViewV1` reduced synchronously from authority under the captured execution frontier; they never consume lagging provider history or a background projector. `IntentDocument` and plans use versioned `HostStateCheckpointV1`, operator observations use append-only `ObservationRecordV1`, metadata names/descriptions remain operator-owned, and semantic counters remain derived. `/transcript` becomes the exact committed semantic transcript while `/session-export` names the presentation/evidence export. Mixed resume is explicitly a legacy base plus exact semantic suffix, full-session exact export remains unavailable, and Web historical output is suffix-only. Audit remains separate with semantic cursor/source-event dedup; the Markdown journal gains machine-readable source provenance. Compatibility `.json`/`.meta.json` dual-write and a rollback mirror continue through Slice 5.6 closeout without old-writer downgrade; sessionless semantic lineage remains deferred. Documentation impact: updated only the OpenSpec task/design/authority requirement and private protocol/decomposition guides. This planning refinement changes no Rust, runtime, command, configuration, public-site, canonical-snippet, or release behavior, so no Unreleased note is added.
- [x] 5.4 Migrate whole-file session snapshots, metadata checkpoints, narrative journal, and audit consumers without conflating their existing authority. Full and mixed authority-backed dispatch and compaction use synchronous exact-frontier semantic context rather than `ConversationState`, projection output, `.json`, `.meta.json`, journal, audit, or advisory `TurnEnd`; mixed context retains one labeled materialized legacy base. One validated reader checks cursor/output identity, digest, revision, replay frontier, chunks, bounds, lineage, and exactness for transcript/frontend consumers, with synchronous replay-derived catch-up for stale UI projections. Versioned host-state checkpoints, append-only observation ledgers, and catalog records preserve operator metadata while deriving semantic counters; full resume ignores rollback-mirror conversation bytes, mixed/legacy resume keeps explicitly labeled compatibility precedence, and replacement validates the complete store set before atomically publishing identity and generation. `/transcript [file|open]` is exact full-lineage output, `/transcript suffix` is the explicitly labeled mixed suffix, and `/session-export` remains presentation/evidence output; private command help is current. TUI, ACP, Web, IPC, telemetry, journal, and audit consumers use semantic lineage/frontiers with generation fences while supervisor completion and authoritative idle queue state retain operator agency independently of advisory `AgentEnd`. Compatibility `.json`/`.meta.json` mirrors continue through Slice 5.6 after semantic durability, semantic writers remain active under rollback, and old-writer downgrade remains denied. Source guards cover all migrated adapters. Documentation impact: reconciled the OpenSpec design/authority requirement, private semantic protocol, decomposition and binary-composition guides, and `[Unreleased]`; applicable public command/session/migration/recovery docs and dual-write closeout remain explicitly deferred to 5.6, so no public site or canonical snippet changed for this task. Sessionless semantic lineage and synthetic facts remain deferred; task 5.5 retains late, lagged, disconnected, restarted, corrupted, and blob-loss consumer campaigns.
- [x] 5.5.0 Freeze the adverse-consumer campaign before task-5.5 runtime mutation. The normative private protocol fixes the typed fault/disposition vocabulary; a 54-scenario pairwise manifest across legacy/mixed/full lineage, attach/lag/disconnect/restart/replacement lifecycle, and exact/projection/frontend/host-record/evidence/mirror consumers; per-consumer fail-closed, degraded, and best-effort laws; an isolated fixture sandbox and deterministic I/O/notification/worker injection boundaries; Linux/macOS/Windows coverage under a 15-second required-CI budget; and operator-agency invariants. Proven corrupt projector-owned chunks may be quarantined and deterministically replaced from validated authority only under the owning projector lock. Replacement validates authority and host stores but may publish with missing/damaged derived projections explicitly unavailable while new-generation workers rebuild. ACP supervisor/worker completion remains canonical despite skipped advisory notifications with bounded notification drain, and IPC lag automatically enqueues reconciled current state. A missing observation ledger degrades open only when durable evidence proves it never existed; malformed/torn ledgers fail closed. Malformed semantic audit rows stop semantic audit advancement and warn without synthetic duplicate/quarantine rows. Journal authority failure is `semantic_source_unavailable`, never sessionless; authority existence with a missing catalog record is fatal. Semantic save followed by compatibility-mirror failure returns explicit `partial_publication` while preserving semantic resumability. The freeze names the current red fixture gaps and permits 5.5 to fix behavior they expose without changing schema/event versions. Task 5.6 alone owns dual-write removal and public/developer compatibility, session, migration, and recovery documentation. Planning-only documentation impact: updated only the task/design/authority requirement and private semantic-protocol/decomposition guides; no Rust, runtime, changelog, public-site, public-doc, command/configuration, or canonical-snippet change.
- [x] 5.5 Execute the frozen 54-scenario adverse-consumer campaign, add the named fixtures and harness, and fix only behavior exposed by its red cases without weakening authority, exactness, operator agency, or compatibility laws. Every manifest row maps to an exhaustive concrete executor that asserts its frozen lineage, lifecycle, fault, consumer, and disposition; AC13 deletes and reconstructs an immutable chunk from a chunk-bearing mixed-lineage authority/projection fixture; and the focused serial GitHub Actions matrix covers Ubuntu and Windows. Required macOS, Ubuntu, and Windows campaign evidence is recorded below and remains within budget. Public/developer docs and dual-write closeout remain 5.6.

  Platform evidence: macOS local (2026-08-22) `cargo test -p omegon --bin omegon session_recovery_campaign:: -- --test-threads=1 --nocapture` passed all 8 campaign tests in 4.79 seconds. GitHub Actions run `32622078435` at `b788f3b8` (2026-08-23) passed all 8 applicable Ubuntu tests in 0.52 seconds and all 7 applicable Windows tests in 5.71 seconds.
- [x] 5.6.0 Freeze the compatibility-publication closeout before task-5.6 runtime mutation. A semantic session is self-sufficient when it is full lineage or mixed lineage with exactly one durable content-addressed `legacy-compatibility-base-v1`; those sessions stop rewriting `.json`/`.meta.json`. Legacy sessions and mixed sessions not yet carrying that base retain the schema-v1 pair strictly as a one-way import source. Opening a valid pair beside pre-boundary authority materializes the compatibility LLM view exactly once and fixes mixed lineage before any full-spine step; afterward pair absence or stale bytes cannot affect semantic context, transcript, or resume. Existing pair files are not automatically deleted. Maintenance inventory, inspect, and quarantine become catalog-first with pair fallback and identity-pin the selected framing. No runtime rollback-source selector is introduced or claimed. Event v1, reducer/cache v5, cursor v1, projection v1, and task-5.4 host schemas remain unchanged. Required docs cover event/replay/persistence compatibility, legacy/mixed/full resume, one-way migration, exact transcript versus presentation export, and fail-closed versus rebuildable recovery. Canonical slash snippets and public docs tests move with the behavior. Planning-only documentation impact: updated the task/design/authority requirement and private semantic protocol; no Rust, command syntax, configuration, public site, canonical generated snippet, or release behavior changed.
- [x] 5.6 Execute the frozen compatibility-publication closeout and co-deliver event/replay/persistence compatibility docs plus public session resume, migration, and recovery documentation. Valid legacy pairs materialize one content-addressed compatibility base before full-spine work and establish mixed lineage; full and materialized-mixed sessions stop requiring or rewriting the pair, while legacy and not-yet-materialized mixed sessions retain it only as a one-way importer. Maintenance inventory, inspect, and quarantine prefer identity-pinned semantic catalog framing with legacy-pair fallback. Existing compatibility artifacts are not automatically deleted, no rollback selector is invented, and accepted event/reducer/cursor/projection/host schema versions remain unchanged. Public sessions, commands, migration, recovery, TUI, and FAQ guidance plus the developer persistence guide and canonical transcript/session-export snippets now state the exactness and recovery boundaries. Focused migration, publication, maintenance, site, Omegon Windows cross-target compile, recovery-campaign, clippy, and multi-crate landing gates pass; native maintenance remains a Unix/WSL surface.

## 6. Optional domain extraction
<!-- specs: kernel-composition/documentation, runtime-contributions/content-packs, runtime-contributions/lifecycle, runtime-invocation/leases -->

- [x] 6.1.0 Freeze the declared in-process service contract and first proof lane before Slice-6 runtime mutation. In-process services are release-coupled, statically linked contributions, not kernel services or third-party plugins. A candidate pairs each bindingless `in_process_service` capability with exactly one typed implementation under one contribution generation; graph validation, implementation parity, readiness, dependency activation, and registry publication are atomic. Typed handles retain capability, owner, and generation identity and are captured only at boot or a declared quiescent boundary. Required and optional service dependencies participate in graph health; optional absence or failure degrades locally without fabricating an active service. Every owned task, subscription, or durable writer requires generation-bound cleanup evidence before retirement, while the first proof lane uses strict no-resource teardown. The first lane is read-only plans/work aggregation over `styrene-work-model` and `styrene-work-runtime`: it isolates source errors as warnings, publishes immutable aggregate snapshots, and supplies shared plan/ACP reads while leaving session-local `IntentDocument` plan mutation, Workbench DTOs, durable session schemas, commands, and frontend wire shapes unchanged. Memory, lifecycle, behavior, context/compaction, codescan, and Git follow only after the service substrate and proof lane pass absence, stale-generation, rollback, and teardown tests. Planning-only documentation impact: updated the task/design/lifecycle requirement and private decomposition guide; no Rust, runtime, command, configuration, public site, canonical snippet, or release behavior changed.
- [x] 6.1.1 Land the no-resource in-process service publication substrate and work-runtime source isolation without registering a production optional domain. `in_process_service` capabilities now carry a stable interface identity and one object-safe typed implementation; EventBus freezes graph-visible dependencies, validates complete implementation parity, atomically publishes generation-tagged handles and capability inventory, preserves the previous registry on rejection, and rejects capability/interface/type/implementation/service-set changes under an unchanged contribution generation. This substrate accepts only strict zero-timeout no-resource read services; resource-bearing services remain blocked on lifecycle-owned drain and cleanup. `styrene-work-runtime` now converts source errors into bounded warnings, preserves prior snapshots as stale, and continues refreshing other sources. Focused Omegon, traits, and work-runtime tests pass; no command, frontend, durable session schema, maintenance boundary, or public documentation changes.
- [x] 6.1.2 Register the first production no-resource domain without cutting over readers. Omegon-owned OpenSpec/design adapters normalize repository changes, tasks, design nodes, and questions into one immutable `styrene-work-runtime` snapshot; absent sources and malformed design artifacts remain source-local warnings. The statically linked `work-aggregation` contribution publishes that snapshot atomically as `service:work-snapshot` / `interface:styrene-work-snapshot-v1` with optional, boot-only, strict zero-timeout teardown. Existing direct plan/ACP readers, `IntentDocument` authority, Workbench DTOs, durable schemas, commands, and frontend wire shapes remain unchanged for the next bounded cutover.
- [x] 6.1.3 Add the shared semantic compatibility projection before switching readers. OpenSpec task facets retain exact stable-ID findings and deterministic source order while duplicate markers receive unique normalized work IDs. `surfaces::plans` derives the existing plan registry, task projection, revisions, source anchors, and identity findings from the immutable work snapshot with parity coverage against the direct compatibility scanner. No production reader is switched by this checkpoint.
- [x] 6.1.4 Capture the accepted work service once at boot and cut over shared readers. Interactive, daemon, headless, bounded, Sentry, and ACP loop hosts carry the same immutable generation through `LoopCompatibilityBindings`; ACP transfers the captured handle across its worker boundary. CLI/loop plan surfaces and ACP plan/task reads derive from `surfaces::plans`, while service absence preserves session-local `IntentDocument` plans and emits empty repository arrays without direct filesystem fallback. Existing commands, DTOs, durable schemas, and frontend wire shapes remain unchanged.
- [x] 6.1.5.0 Freeze the behavior-policy service boundary before task-6.1.5 runtime mutation. The release-coupled `behavior-policy` contribution is an optional, boot-captured, synchronous `service:behavior-policy` / `interface:omegon-behavior-policy-v1` service with strict zero-timeout no-resource teardown. Its object-safe contract accepts immutable host-normalized per-turn policy views and returns unpinned task-mode inference, phase, drift, progress/evidence, first-turn/execution/continuation-pressure, pressure/meta-message, substantive-prose, and pathological-meta-response decisions without retaining state. Explicit operator mode parsing and correction recovery, declared tool capabilities, authoritative observation normalization, `IntentDocument`, persisted `TaskMode`, `ControllerState`, stuck/dead-mouse/meta counters, tool execution, event emission, and nudge insertion remain session/host owned. Normal composition must preserve canonical fixtures BP01-BP09 pinned to commit `9c3a9860` and materialize literal shared vectors before direct-call removal. Absence leaves ordinary text and tool turns callable, preserves explicit and existing session intent, holds controller and recovery counters, disables only behavior-policy-derived first-turn/execution/evidence/continuation nudges and meta retry, and performs no ambient service lookup or direct classifier fallback; host-owned operator-correction recovery, completion reconciliation, plan reminders, stuck recovery, and text-only recovery remain unchanged. `LoopCompatibilityBindings` carries an optional generation-tagged binding through interactive, daemon/control, headless, bounded, Sentry, and ACP hosts, including ACP worker transfer; absence is `None` plus graph degradation and never fabricates service identity. Independent sessions sharing a present service retain independent state. The service owns no task, subscription, process, temporary artifact, or durable writer. Memory, lifecycle, context/compaction, codescan, and Git remain blocked on generation-bound resource drain and cleanup. Planning-only documentation impact: updated the task/design/lifecycle requirement and private decomposition guide; no Rust, runtime, command, configuration, public site, canonical snippet, durable schema, frontend wire shape, or release behavior changed.
- [x] 6.1.5 Publish and cut over the frozen stateless behavior-policy service. `service:behavior-policy` / `interface:omegon-behavior-policy-v1` now publishes atomically with strict no-resource teardown; its boot-captured binding retains capability, owner, generation, and implementation identity across interactive, daemon/control, headless, bounded, Sentry, and ACP execution. Host-normalized immutable views drive unpinned task-mode, phase, drift, progress/evidence, pressure, substantive-prose, exact-message, and pathological-meta decisions with BP01-BP09 literal/parity coverage. Explicit mode/correction, tool capabilities, observations, intent, controller/recovery state, execution, events, and nudge insertion remain host-owned. Absence is `None` plus a graph warning, holds policy counters, preserves ordinary text/tool turns and existing intent, disables only policy-derived pressure/meta retries, and performs no direct fallback or ambient lookup. Existing commands, configuration, durable schemas, frontend wire shapes, and public site/snippets remain unchanged.
- [x] 6.1.6.0 Freeze generation-bound drain and cleanup before admitting a resource-bearing production service. Preserve the existing raw-`Arc`, strict zero-timeout `no_resource_read_service` path and its work/behavior semantics. A distinct managed path keeps the implementation private behind an object-safe `Request`/`Response`/`Error` contract; `ManagedServiceHandle::invoke(request)` runs only in a generation-owned task so success, error, cancellation, panic, and deadline abort settle accounting without detached work. All handles consult one shared admission table keyed by contribution generation. After fallible preparation, one table replacement is the global publication point for unchanged, draining, and new generations: pre-point failure returns rejected with rollback evidence, while post-point cleanup degradation returns published and cannot claim rollback. Active-call and cleanup deadlines are sequential: table replacement starts the call deadline, remaining call tasks are cancelled/aborted/joined, then one non-resetting cleanup deadline applies stop, conditional force-stop, and settlement in reverse topological order from a validated dependency DAG. Strict cleanup requires positive settlement before cleanup-settled, terminal-failure, retired, or ownership release; timeout/failure is nonterminal degraded with retained owner and bounded attempted-cleanup evidence, never unverified. Best-effort cross-boundary resources may be unverified. Changed/new boot-only services are always rejected after first publication; quiescent-declared replacement requires a current runtime/session-bound one-use proof, with no production issuer or migration command. EventBus retains all lifecycle/resource records and cleanup tasks, serializes replacement with explicit async shutdown, and prevents caller cancellation from detaching cleanup. Degraded shutdown leaves process ownership evidence for maintenance/stale pruning; Drop only requests cancellation/force-stop and cannot claim settlement. Synthetic task/subscription/process-tree/durable-writer/temporary-directory controllers prove RG01-RG12 rollback, races, drain, timeout, dependency order, retry, shutdown, diagnostics, and no-resource regressions before codescan becomes the first production managed lane. Planning-only documentation impact: updated the task/design/lifecycle requirement and private decomposition guide; no Rust, runtime, domain, command, configuration, public site, canonical snippet, durable session schema, frontend wire shape, or release behavior changed.
- [x] 6.1.6.1 Land the managed call primitive: object-safe request/response/error contracts, one shared generation admission table, generation-owned call tasks, panic/cancellation accounting, typed stale-handle states, and retirement gates that require joined calls.
- [x] 6.1.6.2 Land generation-owned resource controllers and bounded cleanup: frozen dependency-DAG validation, reverse-topological stop/conditional-force/settlement, sequential call and cleanup deadlines, retained strict degradation and retry, best-effort unverified evidence, and positive-settlement ownership release.
- [x] 6.1.6.3 Add the EventBus-owned synthetic managed-service sidecar: one admission-table publication point, exact unchanged-generation transfer, bounded rejected-candidate cleanup, retained replacement cleanup, managed handle lookup, and cancellation-safe idempotent managed shutdown without graph declarations or a production domain.
- [x] 6.1.6.4 Integrate generation-level managed declarations with candidate-graph preparation and additive async finalization. Validate graph/implementation/resource/policy parity before one whole-table commit, derive owner and composition identity from the prepared graph, reject unsupported boot/quiescent replacement before admission closes, and preserve the synchronous no-resource finalizer.
- [x] 6.1.6.5 Project actual managed lifecycle/resource/cleanup evidence, wire explicit managed shutdown through every normal host before runtime ownership release, and close RG01-RG12 without migrating a production domain. Bounded DTO-only diagnostics retain published/rejected attempt history and actual lifecycle/resource stop, force-stop, settlement, rejection, degradation, and retirement evidence. Interactive, daemon, headless, bounded, Sentry, ACP worker, cleave, and injected-runtime ownership funnels await idempotent managed shutdown before runtime ownership release; source guards preserve that coverage. Focused managed diagnostic, graph, sidecar, best-effort retry, host-wiring, formatting, Clippy, and full Omegon crate gates pass.
- [x] 6.1.6 Implement the frozen managed-service gate, generation resource owner, bounded cleanup engine, boundary enforcement, lifecycle diagnostics, explicit shutdown, and synthetic-resource campaign without migrating a production domain.
- [x] 6.1.7.0 Freeze the first production managed-service lane before codescan mutation. `feature:codescan` publishes optional boot-only `service:codescan` / `interface:omegon-codescan-v1`; one serial worker owns SQLite, scanning, HEAD freshness, and BM25 for tools and code-context requests. Consumers retain one boot-captured managed handle and typed absence without direct fallback. The strict `Task` worker depends on the strict `DurableWriter`, uses 30-second call drain and 5-second cleanup deadlines, and must join before connection settlement. Incremental path replacements are atomic, pruning and HEAD metadata publish only after a complete run, and cancelled full invalidation preserves the complete prior index through one transaction. Compatibility fixtures retain tool schemas, filtering, ranking, context diversity, and supported languages. Documentation impact includes the private lifecycle/design artifacts, developer search/task policy, public tool availability wording, and `[Unreleased]`; no command syntax, configuration schema, durable session schema, or canonical snippet changes.
- [x] 6.1.7.1 Make codescan persistence transaction-safe: normalize repository-relative file state, retain hashes for zero-chunk files, atomically replace code and knowledge paths, defer pruning/HEAD publication until successful completion, and make full invalidation all-or-nothing under cancellation and failure injection.
- [x] 6.1.7.2 Add one managed codescan service with a serial blocking worker, strict worker/writer resource DAG, cooperative queued/active cancellation, owned HEAD freshness work, explicit connection closure, candidate rollback, exact-generation transfer, and stale-handle coverage.
- [x] 6.1.7.3 Cut `codebase_search`, `codebase_index`, and `request_context(kind="code")` over to one boot-captured managed handle across interactive, daemon, headless, bounded, Sentry, and ACP execution. Preserve typed absence and unrelated-context continuity without ambient lookup or direct SQLite fallback.
- [x] 6.1.7.4 Run the real-domain RG campaign, parity fixtures, direct-writer source guards, Windows reopen/delete closure proof, `just test-crate omegon-codescan`, full Omegon crate tests, formatting, and Clippy.
- [x] 6.1.7.5 Reconcile the decomposition guide, codescan/search and task-ownership docs, public tool availability, OpenSpec tasks, and `[Unreleased]`, then mark Slice 6.1.7 complete.
- [x] 6.1.7 Publish codescan as the first production managed-service lane with one serial owner, transactional cancellation, boot-captured consumers, strict worker/writer settlement, typed optional absence, compatibility coverage, and reconciled documentation.
- [x] 6.1.8.0 Freeze the lifecycle/OpenSpec managed-service boundary before runtime mutation. `feature:lifecycle` publishes optional boot-only `service:lifecycle` / `interface:omegon-lifecycle-v1` under contribution generation `contribution:lifecycle-managed-v1`; one serial repository worker owns design/OpenSpec artifact coordination, the opsx FSM/ledger, reconciliation, and recovery. Git-native artifacts remain semantic content authority while the ledger remains enforcement/audit state. Versioned requests cover immutable design/OpenSpec snapshots and queries, typed design/OpenSpec mutations, explicit audit/recovery, expected revisions, and idempotent operation identity; responses return the observed/committed revision, resulting projection, bounded warnings/drift, and typed effects or errors. Session focus, context TTL, tool/command rendering, authorization, memory requests, frontend state, TDD evidence, Codex export, and offline migration remain outside. The strict `Task` worker depends on a strict `DurableWriter`; 30-second call drain and 5-second cleanup deadlines require the worker to stop and join after all journal/artifact/ledger writes settle. External Markdown authoring is allowed but invalidates the repository revision and must be reconciled before a managed mutation. Absence keeps lifecycle tools and semantic surfaces declared with typed unavailable state, omits only lifecycle-derived context/work data, and performs no direct scan, ledger load, or filesystem fallback. Documentation impact: updated the private lifecycle/design requirements and durable decomposition guide; this planning-only freeze changes no Rust, runtime behavior, command/configuration syntax, public site, canonical snippet, or release behavior.
- [x] 6.1.8.1 Harden `omegon-opsx` persistence before managed publication. `Lifecycle` now retains the last positively persisted snapshot and restores the complete state and audit log after every save failure. The mandatory state-store API advances one serde-default monotonic revision under a cross-platform exclusive lock, rejects stale and non-monotonic writers, writes through a unique temporary file with pre/post-replacement flush and supported parent-directory sync, and preserves legacy revision-zero loading. Normal setup uses fallible lifecycle construction, so corrupt or future state disables the optional feature locally instead of retrying the same file and panicking. Reopen, failed-save, stale-owner, legacy, corrupt/future-state, full Omegon, Windows cross-target, formatting, and Clippy gates pass. Documentation impact: updated the durable decomposition guide and `[Unreleased]`; the additive ledger revision is backward-readable and no command, configuration, public site, canonical snippet, durable session schema, or frontend wire shape changed.
- [x] 6.1.8.2 Add the managed lifecycle repository worker, strict worker/writer resource DAG, boot-only contribution, revisioned read requests, explicit recovery requests, candidate rollback, exact-generation transfer, stale-handle behavior, and typed absence without exposing `Lifecycle`, `JsonFileStore`, `OpenSpecRepository`, design repositories, or provider locks. One bounded serial worker now owns the loaded ledger and design/OpenSpec repositories, returns owned DTOs with deterministic ledger/artifact/transaction revisions, rejects conflicting populated OpenSpec roots before compatibility registration, and fails closed when recovery requires the root-aware journals deferred to 6.1.8.4. Focused lifecycle-service tests, full Omegon crate tests, Windows GNU cross-target checking, formatting, diff hygiene, and Clippy pass. Documentation impact: updated the private decomposition/design status and `[Unreleased]`; existing lifecycle consumers and mutation paths remain unchanged until tasks 6.1.8.3-6.1.8.5, with no command, configuration, public-site, canonical-snippet, durable-session-schema, or frontend-wire change.
- [x] 6.1.8.3 Move design mutations behind journaled artifact-plus-ledger transactions, including create, state/question/metadata updates, branch, and implement-to-OpenSpec; preserve Git-native authority and make every crash point recover to a defined pre- or post-operation state. Managed requests now carry a stable operation identity and exact frozen-root, ledger-path/content, artifact, and transaction revision; one repository lock revalidates that revision after staging and before a durable checksummed journal. Canonical artifacts settle before one staged ledger snapshot, all resources preflight before recovery writes, known partial frontiers roll forward deterministically, hostile/unknown frontiers remain quarantined recovery-required evidence, and checksummed receipts provide payload-bound replay without duplicate effects. Canonical drift is reconciled into staged FSM policy; malformed, rewrite-unsafe, conflicting-authority, symlink, stale-revision, and operation-ID-conflict cases fail closed through versioned typed errors. Focused design transaction and lifecycle-service suites, `omegon-opsx`, Windows GNU cross-target checking, formatting, diff hygiene, Clippy, and the full Omegon gate pass. Timeout-sensitive cleave/extension process fixtures use bounded test-only readiness budgets appropriate for full-suite contention without changing production defaults. Documentation impact: updated private architecture/status and `[Unreleased]`; compatibility consumers remain unchanged until 6.1.8.5, while general OpenSpec mutation/archive journals remain 6.1.8.4.
- [x] 6.1.8.4 Move OpenSpec propose, add-spec, task-status/reconciliation, test registration, transition, archive, abandon/reopen, and repository recovery behind revisioned transactions; make archive journals atomic, repo-relative, path-contained, deterministic, and idempotently recoverable. Managed OpenSpec requests now share the frozen repository and ledger locks, global operation-ID namespace, exact expected revision, durable bounded journal/receipt namespace, artifact-first and ledger-last settlement, and typed stale/conflict/recovery errors with design mutations. Archive and reopen carry contained repository-relative source/destination paths plus exact source, moved, and final tree identities; recovery preflights all resources, validates conflicting receipts before mutation, deterministically rolls known partial states forward, and quarantines malformed, oversized, unhealthy, path-tampered, or unknown frontiers. Populated legacy OpenSpec authority remains selected when the primary root contains only metadata. Focused mutation/recovery suites, serialized full Omegon tests, `omegon-opsx`, changed-crate Clippy, formatting, and Windows GNU checking pass. Documentation impact: updated private architecture/status and `[Unreleased]`; compatibility consumers remain unchanged until 6.1.8.5, with no command, configuration, public-site, canonical-snippet, durable-session-schema, or frontend-wire change.
- [x] 6.1.8.5 Cut tools, context, setup snapshots, ACP, TUI, Web, IPC, workflow/Sentry, work aggregation, project rules, session/check-in projections, and Codex export over to boot-captured lifecycle handles or immutable service output. Production setup now registers the managed adapter, captures one exact-generation binding, and refreshes one host-owned immutable repository observation. Lifecycle tools and doctor, context packs, frontend projections, workflow readiness, Sentry hooks, work normalization, rules, prompt guidance, startup state, session/check-in summaries, and Codex export no longer reconstruct repository authority or write canonical lifecycle artifacts directly. Test-only compatibility fixtures remain explicit; external Markdown authoring, append-only TDD evidence, and stopped-runtime migration remain outside the service boundary. Focused lifecycle-service, managed-adapter, work-aggregation, Sentry-hook, project-rules, prompt, startup, session-log, source-guard, formatting, compile, changed-crate Clippy, OpenSpec, and serialized local Omegon gates pass. Task 6.1.8.6 supplies the completed cross-platform real-domain campaign.
- [x] 6.1.8.6 Run state/artifact compatibility, transition, crash-point, concurrency, absence, stale-generation, cleanup, and cross-surface parity campaigns on macOS, Ubuntu, and Windows; add direct-owner/write source guards and reconcile architecture, lifecycle, OpenSpec, operator-facing availability, and `[Unreleased]` documentation. The portable campaign covers concurrent commit/replay races, typed absence with declared tools and unrelated EventBus continuity, strict file-handle settlement, and shared host/work/ACP/IPC/TUI/Web projections. Focused compatibility, transition, design/OpenSpec crash recovery, stale-revision, source-ownership, crate, Clippy, and serialized workspace gates pass locally on macOS. GitHub Actions run `32856627018` at `ff56bed3` passes the serialized lifecycle campaign and source guard on `macos-latest`, `ubuntu-latest`, and `windows-latest`, together with the general Rust unit and integration jobs.
- [x] 6.1.8 Publish lifecycle/OpenSpec as one revisioned managed repository service with crash-recoverable artifact/ledger transactions, boot-captured consumers, strict worker/writer settlement, typed optional absence, and no production direct ownership bypass. Slices 6.1.8.0 through 6.1.8.6, the cross-platform real-domain campaign, and final documentation reconciliation are complete.
- [x] 6.1.9.0 Freeze the memory managed-service boundary before runtime mutation. `feature:memory` publishes optional boot-only `service:memory` / `interface:omegon-memory-v1` under contribution generation `contribution:memory-managed-v1`; one serial worker owns the selected project store, optional global store, SQLite connections and WAL state, durable facts/minds/edges/episodes/vectors, JSONL import/export, and configured Codex-vault synchronization. Versioned requests return owned DTOs and cover status, retrieval, durable mutation, embedding-result storage, episode storage, synchronization, and bounded maintenance. Mutations carry stable operation identity; content-addressed stores remain naturally idempotent, targeted mutations use entity-specific version preconditions, and JSONL import preserves the existing Lamport conflict policy rather than imposing one global store revision. The host retains session-local working-memory pins, selected mind, context TTL/hash/presentation policy, tool rendering and authorization, provider/model selection, extraction and embedding computation, and compaction. Host-owned asynchronous generation must settle before managed shutdown and can persist results only through the captured handle. The strict `resource:memory-worker` `Task` depends on strict `resource:memory-writer` `DurableWriter`; a 30-second call-drain deadline and one 5-second cleanup deadline require queued/active work to stop and the worker to join before all SQLite/WAL and filesystem writes settle. Existing root selection, schema/migration, persisted/wire vocabulary, SQLite/in-memory parity, FTS fallback, ranking, decay, mind, JSONL, vault, tool, context, and surface behavior remain compatibility requirements. Stopped-runtime schema/root migration remains outside; one-shot embedding backfill must use a bounded managed composition. Absence keeps memory tools and status surfaces declared with typed unavailable state, omits durable memory context, preserves unrelated context and host compaction, and performs no direct store, JSONL, or vault fallback. Documentation impact: updated the private lifecycle/design requirements and durable decomposition guide; this planning-only freeze changes no Rust, runtime behavior, command/configuration syntax, public site, canonical snippet, persisted schema, or release behavior.
- [x] 6.1.9.1 Harden the memory persistence boundary for managed publication. Schema v8 adds compact payload-bound operation receipts and durable episode metadata with governed v5-v7 migration. The additive `MemoryMutation` contract supplies stable operation identity, entity-version preconditions, exact replay, and payload-conflict rejection without duplicating fact content or vectors into receipts. Existing compatibility methods now make mind-plus-record, batch status, supersede, vector-plus-metadata, episode, and JSONL effects transactional; v5/v6 `default` records still migrate to `legacy`, while v7 records reconcile to `primensus`. SQLite and in-memory backends preserve per-record Lamport conflict behavior and high-water marks, batch rollback, JSONL edge/episode idempotency, episode metadata, edge validation, deterministic FTS tie ordering, and stable embedding metadata. Shared parity, operation replay/conflict, stale-version, JSONL rollback/high-water, deterministic fallback, metadata, file-reopen, injected rollback, schema-contract, migration, startup, and legacy-root reopen tests pass. Documentation impact: updated the schema authority, project-memory behavior, private decomposition status, and `[Unreleased]`; no command/configuration syntax, public site, canonical snippet, or frontend wire shape changed.
- [ ] 6.1.9.2 Add the boot-only managed memory service with one serial worker, strict worker/writer resource DAG, project and optional global store ownership, owned request/response DTOs, cancellation, candidate rollback, exact-generation transfer, stale-handle behavior, and typed absence. Keep provider-backed extraction and embedding computation outside the service while accepting their durable results only through the managed handle.
- [ ] 6.1.9.3 Move JSONL import/export and configured Codex-vault synchronization behind the memory worker. Preserve path containment, idempotent repeated synchronization, transient-read safety, existing selected-root compatibility, and strict settlement of SQLite/WAL and filesystem effects before writer retirement.
- [ ] 6.1.9.4 Cut memory tools, context injection, lifecycle ingestion, session-end episode/fact persistence, automatic embedding result writes, status projections, and one-shot embedding backfill over to boot-captured handles or bounded managed compositions. Preserve session-owned pins, mind selection, context and compaction policy, provider selection, extraction/embedding computation, command/DTO schemas, and unrelated-context continuity without ambient lookup or direct persistence fallback.
- [ ] 6.1.9.5 Run persistence, retrieval, mind, episode, JSONL, vault, concurrency, cancellation, absence, stale-generation, cleanup, and cross-surface compatibility campaigns on macOS, Ubuntu, and Windows; add direct-owner/write source guards and reconcile memory architecture, operator-facing availability, task ownership, public behavior, and `[Unreleased]` documentation.
- [ ] 6.1.9 Publish memory as one managed durable service with boot-captured consumers, deterministic no-vector degradation, strict worker/writer settlement, typed optional absence, and no production direct ownership bypass.
- [ ] 6.1 Convert memory, lifecycle, plans/work, behavior, context/compaction, codescan, and Git integration to declared in-process services.
- [ ] 6.2 Remove concrete feature imports from semantic surfaces and bind TUI, ACP, Web, IPC, CLI, and daemon to shared snapshots/actions.
- [ ] 6.3 Unify native extension, MCP, and manifest discovery under contribution lifecycle while retaining transport-specific adapters.
- [ ] 6.4 Move shipped skills, prompts, personas, tones, workflows, and catalog data into independently versioned content packs.
- [ ] 6.5 Prove each optional domain can be absent or degraded without blocking the maintenance executable or constitutional kernel.
- [ ] 6.6 Co-deliver per-domain architecture, contribution-pack, absence/degradation, and applicable public feature documentation in each extraction lane.

## 7. Release composition, budgets, and deletion
<!-- specs: kernel-composition/documentation, kernel-composition/maintenance, kernel-composition/release-locks, runtime-contributions/lifecycle -->

- [ ] 7.1 Produce signed contribution locks containing identity, artifact digest, protocol range, target support, required/optional status, and fallback behavior.
- [ ] 7.2 Gate maintenance, interactive, headless, daemon, and full composition matrices across supported packaging paths.
- [ ] 7.3 Enforce dependency, binary-size, startup-task, schema-token, resident-capability, and default-callable budgets.
- [ ] 7.4 Remove legacy disabled-name sets, collision-by-order, duplicate supervisor/command authorities, and surface-specific capability allowlists.
- [ ] 7.5 Run broad Rust gates, package/link verification, real maintenance diagnosis, denial/quarantine, stale-record-pruning, audit, and offline-verification exercises, and normal harness exercises.
- [ ] 7.6 Reconcile OpenSpec, design, Workbench, changelog, and release evidence before archive.
- [ ] 7.7 Validate the complete public docs site, canonical snippets, packaging/install guidance, migration notes, and cross-surface terminology against released artifacts.
