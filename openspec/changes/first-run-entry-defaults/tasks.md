# Implementation tasks

## 1. Quiet first launch
<!-- specs: fresh-startup -->

- [x] Reproduce the legacy menu with a profile-free, non-child captured launch.
- [x] Remove the blocking wizard and automatic posture write; retain splash detection.
- [ ] Verify both entrypoint defaults with zero-inference startup captures.
- [ ] Run the omegon crate gate, changed Clippy, script tests, and OpenSpec validation.
- [ ] Remove the mistakenly created local posture override and refresh the existing preview.
